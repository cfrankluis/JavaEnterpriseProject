server:
  port: 9022

spring:
  datasource:
    url: jdbc:postgresql://${datachanURL}/mydatabase
    username: ${datachanUsername}
    password: ${datachanPassword}
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: create
      
      
      
      
spring.mail.host: smtp.gmail.com
spring.mail.port: 587
spring.mail.username: socialmedianow63@gmail.com
spring.mail.password: bkswsjjigiakypnj
spring.mail.properties.mail.smtp.auth: true #needs to be here for email to work, ignore the warning
spring.mail.properties.mail.smtp.starttls.enable: true #needs to be here for email to work, ignore the warning


#jdbc:postgresql://${datachanURL}/mydatabase

#${datachanUsername}
#${datachanPassword}